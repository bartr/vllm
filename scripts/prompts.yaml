# Sample prompt list for `ask --bench --file scripts/prompts.yaml`.
#
# Each entry must include `prompt`. Optional per-prompt overrides:
#   dsl          DSL directives prepended as ":dsl <text>\n" before the prompt.
#                When --dsl is also passed on the CLI, both are merged.
#   max_tokens   Override --max-tokens for this prompt only.
#   temperature  Override --temperature for this prompt only.

- prompt: "Explain Azure"

- prompt: "What is the capital of Texas?"

- prompt: "Explain Kubernetes"
