apiVersion: v1
kind: Service
metadata:
  name: cllm
  namespace: cllm
  labels:
    app.kubernetes.io/name: cllm
spec:
  selector:
    app: cllm
  ports:
    - name: http
      port: 8080
      targetPort: http
